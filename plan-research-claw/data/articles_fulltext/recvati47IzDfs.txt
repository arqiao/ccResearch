# OpenClaw 极简上手指南：让你的 AI 助手连接微信、WhatsApp、Telegram
# URL: https://mp.weixin.qq.com/s/9di74zwUDdg5MNEV32PPZw
# 日期: 2026-02-06
# 来源: 微信-探奇猫
# 分类: 生态集成 / 
# 字数: 7290

你是否想过拥有一个可以在微信、WhatsApp、Telegram 上随时召唤的 AI 助手？本文将手把手教你用 OpenClaw 搭建一个全平台通用的 AI 助手。
一、OpenClaw 是什么？
OpenClaw 是一个开源的 AI 助手网关项目，它的核心能力是：
•
多平台连接
：支持 WhatsApp、Telegram、Discord、iMessage 等主流通讯工具
•
AI 集成
：内置支持 Pi（Claude Code 的本地版本）作为推理引擎
•
安全沙箱
：每个对话都在独立的工作空间中运行，保护你的隐私
•
跨设备同步
：可以在 iOS/Android 设备上使用，甚至通过菜单栏应用唤醒
简单来说，OpenClaw 就像一个「AI 路由器」，你通过任意聊天软件发送消息，它会把请求转发给后端的 AI 模型，再把回答返回给你。
二、安装准备
环境要求
• Node.js >= 22（推荐使用 nvm 管理）
• macOS / Linux / WSL2（Windows 用户推荐 WSL2）
• Brave Search API key（可选，用于网页搜索功能）
一键安装
# 使用官方安装脚本（推荐）
curl -fsSL https://openclaw.ai/install.sh | bash
# 或者通过 npm 全局安装
npm install -g openclaw@latest
pnpm add -g openclaw@latest
安装完成后，验证安装：
openclaw --version
三、快速开始（5 分钟上手）
步骤 1：运行向导配置
OpenClaw 提供了一个交互式向导，帮你完成初始配置：
openclaw onboard --install-daemon
向导会引导你完成以下配置：
•
模型认证
：推荐使用 Anthropic API Key（Claude 模型）
•
平台连接
：WhatsApp 扫码登录、Telegram Bot Token 等
•
安全设置
：默认开启陌生人消息过滤
•
工作空间
：自动创建你的个人工作目录
步骤 2：启动网关服务
配置完成后，网关服务已经在后台运行。你可以手动启动查看日志：
# 查看服务状态
openclaw gateway status
# 手动启动（前台运行，方便调试）
openclaw gateway --port 18789 --verbose
服务启动后，访问本地控制台：
• 网址：http://127.0.0.1:18789/
• 这是一个 Web 界面，你可以直接在里面与 AI 对话
步骤 3：连接你的聊天平台
WhatsApp 方式
# 登录 WhatsApp
openclaw channels login
然后在 WhatsApp 中打开「设置 → 关联设备 → 扫描二维码」。
Telegram 方式
在 Telegram 中搜索 @BotFather，创建新机器人并获取 Token，然后在向导中配置。
四、实战案例
案例 1：打造「每日科技新闻播报员」
需求
：每天早上 9 点自动发送 Hacker News 和 GitHub Trending 到群聊
实现方案
：
1. 创建一个 Python 脚本获取新闻：
#!/usr/bin/env python3
import
urllib.request
import
json
from
datetime
import
datetime
def
get_hackernews_top5
():
"""获取 Hacker News Top 5"""
ids = json.loads(urllib.request.urlopen(
"https://hacker-news.firebaseio.com/v0/topstories.json"
, timeout=
10
).read())[:
5
]
stories = []
for
item_id
in
ids:
item = json.loads(urllib.request.urlopen(
f"https://hacker-news.firebaseio.com/v0/item/
{item_id}
.json"
, timeout=
10
).read())
stories.append(
f"•
{item[
'title'
]}
\n  👍
{item.get(
'score'
,
0
)}
"
)
return
stories
def
generate_message
():
today = datetime.now().strftime(
"%Y-%m-%d"
)
hn = get_hackernews_top5()
message =
f"📅
{today}
每日技术资讯\n\n🔥 Hacker News Top 5\n"
message +=
"\n"
.join(hn)
return
message
if
__name__ ==
"__main__"
:
print
(generate_message())
2. 设置定时任务（OpenClaw 内置 Cron 支持）：
# 每天 UTC+8 9 点执行（UTC 1:00）
openclaw cron add \
--name
"每日技术资讯推送"
\
--schedule
"0 1 * * *"
\
--payload
'{"kind": "agentTurn", "message": "执行新闻脚本并发送到群组"}'
3. 在 OpenClaw 中配置定时触发，调用上面的脚本并发送到目标群组。
效果
：每天早上 9 点，你的群聊里就会自动收到当天的科技新闻摘要。
案例 2：搭建「智能日程助手」
需求
：通过微信消息添加日程，并在当天提醒
实现方案
：
#!/usr/bin/env python3
import
json
from
datetime
import
datetime, timedelta
# 简单的日程存储
SCHEDULE_FILE =
"/home/node/.openclaw/workspace/schedule.json"
def
add_event
(
message
):
"""解析消息并添加日程"""
# 格式示例："明天下午3点开会"
parts = message.split(
"点"
)
if
len
(parts) >=
2
:
# 解析时间并保存
event = {
"content"
: parts[
1
].strip()
if
parts[
1
]
else
"待办"
,
"time"
: parts[
0
] +
"点"
}
# 保存到文件
with
open
(SCHEDULE_FILE,
'a'
)
as
f:
f.write(json.dumps(event) +
"\n"
)
return
f"已添加：
{event[
'time'
]}
{event[
'content'
]}
"
return
"格式不对哦，请用「明天下午3点开会」的格式"
def
get_today_events
():
"""获取今天的所有日程"""
events = []
if
os.path.exists(SCHEDULE_FILE):
with
open
(SCHEDULE_FILE,
'r'
)
as
f:
for
line
in
f:
if
line.strip():
events.append(json.loads(line))
return
events
OpenClaw 技能配置
（
~/.openclaw/workspace/skills/schedule-skill/skills.json
）：
{
"name"
:
"schedule-assistant"
,
"description"
:
"智能日程管理助手"
,
"triggers"
:
[
"添加日程"
,
"查看日程"
]
,
"actions"
:
[
{
"name"
:
"add_event"
,
"command"
:
[
"python3"
,
"/path/to/schedule_skill.py"
]
}
]
}
案例 3：实现「文件搜索机器人」
需求
：在群聊中说「搜索代码」，AI 自动搜索工作区中的相关文件
实现方案
：
#!/usr/bin/env python3
import
os
import
subprocess
def
search_files
(
query, extensions=[
"py"
,
"js"
,
"md"
]
):
"""在工作区中搜索文件"""
results = []
workspace =
"/home/node/.openclaw/workspace"
for
ext
in
extensions:
cmd =
f"find
{workspace}
-name '*.
{ext}
' -exec grep -l '
{query}
' {{}} \\;"
try
:
output = subprocess.check_output(cmd, shell=
True
, text=
True
)
results.extend(output.strip().split(
'\n'
))
except
:
pass
return
results[:
5
]
# 最多返回 5 个结果
if
__name__ ==
"__main__"
:
import
sys
query = sys.argv[
1
]
if
len
(sys.argv) >
1
else
""
results = search_files(query)
if
results:
print
(
f"找到
{
len
(results)}
个相关文件："
)
for
r
in
results:
print
(
f"  •
{r}
"
)
else
:
print
(
"未找到相关文件"
)
配置 OpenClaw 技能
：
在
~/.openclaw/workspace/skills/
目录下创建
file-search/
目录，添加
SKILL.md
：
# File Search Skill
搜索工作区中的文件内容。
## Usage
-
"搜索 xxx" - 搜索包含 xxx 的文件
-
"找代码 xxx" - 查找代码文件
案例 4：搭建「社交网络机器人」（Moltbook）
需求
：在 Moltbook（AI 社交网络）上自动发布和互动
实现方案
：
#!/usr/bin/env python3
import
urllib.request
import
json
API_KEY =
"your_moltbook_api_key"
BASE_URL =
"https://www.moltbook.com/api/v1"
def
post_message
(
title, content, submolt=
"general"
):
"""发布帖子"""
data = json.dumps({
"title"
: title,
"content"
: content,
"submolt"
: submolt
}).encode()
req = urllib.request.Request(
f"
{BASE_URL}
/posts"
,
data=data,
headers={
"Authorization"
:
f"Bearer
{API_KEY}
"
,
"Content-Type"
:
"application/json"
}
)
response = urllib.request.urlopen(req, timeout=
10
)
return
json.loads(response.read())
def
get_feed
(
limit=
10
):
"""获取热门动态"""
url =
f"
{BASE_URL}
/feed?sort=hot&limit=
{limit}
"
req = urllib.request.Request(url, headers={
"Authorization"
:
f"Bearer
{API_KEY}
"
})
response = urllib.request.urlopen(req, timeout=
10
)
return
json.loads(response.read())
def
upvote
(
post_id
):
"""点赞帖子"""
req = urllib.request.Request(
f"
{BASE_URL}
/posts/
{post_id}
/upvote"
,
method=
"POST"
,
headers={
"Authorization"
:
f"Bearer
{API_KEY}
"
}
)
urllib.request.urlopen(req, timeout=
10
)
OpenClaw 自动化配置
（
~/.openclaw/workspace/skills/moltbook-skill/skills.json
）：
{
"name"
:
"moltbook-assistant"
,
"description"
:
"Moltbook AI 社交网络助手"
,
"triggers"
:
[
"发帖子"
,
"看动态"
,
"点赞"
]
,
"actions"
:
[
{
"name"
:
"post"
,
"command"
:
[
"python3"
,
"/path/to/moltbook.py"
,
"post"
]
}
,
{
"name"
:
"feed"
,
"command"
:
[
"python3"
,
"/path/to/moltbook.py"
,
"feed"
]
}
]
}
五、进阶技巧
1. 远程访问
如果你想让 AI 助手随时随地可用，可以配置远程访问：
# 使用 Tailscale（推荐）
openclaw gateway --
bind
tailnet --token your_tailscale_token
# 或者使用 SSH 隧道
ssh -L 18789:localhost:18789 user@your-server
2. 多代理路由
OpenClaw 支持为不同的聊天对象分配不同的 AI 代理：
{
"routing"
:
{
"agents"
:
{
"main"
:
{
"workspace"
:
"~/.openclaw/workspace"
}
,
"tech-news"
:
{
"workspace"
:
"~/.openclaw/workspace-tech"
,
"model"
:
"claude-sonnet-4-20250514"
}
}
,
"rules"
:
[
{
"chatId"
:
"-1001234567890"
,
"agent"
:
"tech-news"
}
]
}
}
3. 安全审计
定期运行安全检查：
# 快速审计
openclaw security audit
# 深度审计（检查所有配置）
openclaw security audit --deep
六、常见问题
Q1: 微信能用吗？
OpenClaw 本身不直接支持微信，但可以通过
wechat-mcp
插件实现。配置后即可在微信中使用。
Q2: 如何备份数据？
OpenClaw 的数据主要存储在
~/.openclaw/
目录下：
# 备份整个配置
cp
-r ~/.openclaw ~/openclaw-backup-$(
date
+%Y%m%d)
# 只备份工作空间
cp
-r ~/.openclaw/workspace ~/workspace-backup
Q3: 如何更新到最新版本？
# CLI 更新
npm update -g openclaw@latest
# 重启网关服务
openclaw gateway restart
Q4: 消息发不出去怎么办？
1. 检查网关状态：
openclaw gateway status
2. 查看详细日志：
openclaw gateway --verbose
3. 检查配对批准：
openclaw pairing list
Q5: 如何删除某个对话的历史？
# 查看所有会话
openclaw sessions list
# 删除特定会话
openclaw sessions delete <session_id>
七、总结
OpenClaw 的核心优势在于：
1.
统一管理
：一个后端连接多个聊天平台
2.
高度可定制
：通过 Skills 系统扩展功能
3.
安全可靠
：沙箱隔离 + 配对验证
4.
开源免费
：MIT 许可证，社区活跃
通过本文的案例，你应该已经掌握了 OpenClaw 的基本用法。不妨动手试试，搭建一个属于你自己的 AI 助手！
参考资料
：
• OpenClaw 官方文档：https://docs.openclaw.ai/
• GitHub 仓库：https://github.com/openclaw/openclaw
• Moltbook AI 社交网络：https://www.moltbook.com